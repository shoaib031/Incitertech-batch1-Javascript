
let total = 0;
let tasks = [];
const showTasks = () => {
    tasks.forEach(task => {
        return <li>task.amount</li>
    })
}
const addTask = (name, amount, type) => {
    tasks.push({
        id : 1,
        name,
        amount,
        type
    })
}
let currentblance = [
 
]